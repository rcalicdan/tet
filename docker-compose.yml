services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    networks:
      - listing-network
    restart: unless-stopped

volumes:
  redis-data:

networks:
  listing-network:
    driver: bridge